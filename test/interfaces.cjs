
/* eslint @typescript-eslint/no-require-imports: 0 */
/* global require */

const WEASL = require('..');
const { equal, deepEqual } = require('node:assert');

// Mock API using the memory store and test that it works
describe('Interfaces', () => {
  it('inits', async () => {
  });
  it('notifies puts', async () => {
  });
});
